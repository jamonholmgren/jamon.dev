#!/bin/bash
source "./bin/env.sh"

# echo "Starting the server remotely..."
# ssh $SSH_USER@$DOMAIN "$REMOTE_ROOT/qb64_server"

echo "Server should start automatically if you've set up qb64-watcher.service."
echo "Check the comments in that file for how to set it up."
